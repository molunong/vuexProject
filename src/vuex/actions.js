// import * as types from './mutation-types'
// import userinfo from './modules/userinfo'

export default {
  getWechatOpenId ({ commit }) {
    let openid = 'get openid from wechat js sdk'
    console.log('into global.actions.getWechatOpenId')
    // userinfo.actions.setOpenId(openid)
    return openid
  }
}
